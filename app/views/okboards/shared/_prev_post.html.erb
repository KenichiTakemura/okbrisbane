<% if @prev_post.present? %>
<%= link_to(image_tag("#{I18n.locale}/common/btn_prev.gif", :size => "45x25"), Okboard.okboard_link_with_id(@okpage,@prev_post.id,@post_search.id), :class => "to_prev_link")%>
<div class="to_prev_title"><%= @prev_post.subject %></div>
<% else %>

<% end %>
