<%= form_for(@top_feed_list) do |f| %>
  <% if @top_feed_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@top_feed_list.errors.count, "error") %> prohibited this top_feed_list from being saved:</h2>

      <ul>
      <% @top_feed_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
